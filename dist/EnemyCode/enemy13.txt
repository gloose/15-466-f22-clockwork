WHILE (TRUE)
  IF (BRAWLER.ALIVE)
    YORMUN.ATTACK(BRAWLER)
  END
  IF (HEALER.ALIVE)
    YORMUN.ATTACK(HEALER)
  END
  IF (CASTER.ALIVE)
    YORMUN.ATTACK(CASTER)
  END
  IF (RANGER.ALIVE)
    YORMUN.ATTACK(RANGER)
  END
END