WHILE (TRUE)
 IF (PURGEN.HEALTH < PURGEN.HEALTH_MAX)
  VERNIE.HEAL(PURGEN)
 END
 IF (VERNIE.HEALTH < VERNIE.HEALTH_MAX)
  PURGEN.HEAL(VERNIE)
 END
 IF (VERNIE.HEALTH == VERNIE.HEALTH_MAX)
  VERNIE.DESTROY_ALL()
 END
 IF (PURGEN.HEALTH == PURGEN.HEALTH_MAX)
  PURGEN.DESTROY_ALL()
 END
END